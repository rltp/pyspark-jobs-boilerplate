# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py27,py36

[testenv]
extras = dev
commands =
    pylint --rcfile=.pylintrc app
    python -u -m unittest discover "app_tests/units"
    python -u -m unittest discover "app_tests/acceptances"